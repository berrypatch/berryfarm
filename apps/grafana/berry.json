{
    "name": "grafana",
    "description": "Grafana 6 measurement/monitoring service",
    "variables": [
        {
            "name": "PORT",
            "type": "number",
            "description": "The main port Grafana will run on. The Grafana HTTP service will be exposed on this port.",
            "required": true,
            "default": 3000
        }
    ]
}